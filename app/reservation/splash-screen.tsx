import HobinkLogo from '@/components/logo';

const SplashScreen = () => {
  return (
    <main className='h-screen grid w-full place-content-center bg-primaryColor'>
      <HobinkLogo textColor='text-white' />
    </main>
  );
};

export default SplashScreen;
